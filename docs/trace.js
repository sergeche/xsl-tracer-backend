/**
 * Description of generated JSON tracer tag
 */
var tag = {
	/** 
	 * List of external XSL modules used in current transformation.
	 * <em>This property is availbale for root tag only.</em>
	 * @type {String[]}
	 */
	xsl: [],
	
	/** 
	 * List of external XML modules used in current transformation.
	 * <em>This property is availbale for root tag only.</em>
	 * @type {String[]}
	 */
	xml: [],
	
	
	/**
	 * Tag's name
	 */
	name: "div",
	
	/**
	 * Tag type. Could be <code>LRE</code> (Literal Result Element) or 
	 * <code>XSL</code>. LRE means that tag was generated by XSL stylesheet
	 */
	type: "LRE",
	
	/**
	 * Tag XPath in <em>result document</em> (for LRE elements) or in
	 * XSL stylesheet (for XSL elements)
	 */
	xpath: "/html[1]/body[1]/div[2]",
	
	/**
	 * Tag's source: a place in XSL stylesheet where current tag was defined
	 * or outputted
	 */
	src: {
		/** Root tag's resource vocabulary name ('xsl' or 'xml') */
		v: "xsl",
		/** Resource index in vocabulary */
		i: 2,
		/** XPath to element output/definition */
		xpath: "/xsl:stylesheet[1]/xsl:template[1]/div[1]"
	},
	
	/**
	 * Current tag's XML context 
	 */
	ctx: {
		/** Root tag's resource vocabulary name ('xsl' or 'xml') */
		v: "xml",
		/** Line number (if any) */
		l: 11,
		/** Resource index in vocabulary */
		i: 2,
		/** XPath to element definition */
		xpath: "/document/content"
	},
	
	/**
	 * List of children tags
	 * @type {Tag[]}
	 */
	children: []
};